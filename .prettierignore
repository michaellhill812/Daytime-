# Build output and dependencies — nothing here is source.
node_modules
dist
dist-embed
dist-test
dist-badurl
.harness-dist

# Screenshots and other per-run evidence from the test suites.
tests/.out

# Generated by tsc.
tsconfig.tsbuildinfo

# Prose. The wrapping and emphasis in these is deliberate, and Prettier's only
# opinion about them is to swap *emphasis* for _emphasis_ — churn, not style.
*.md
